# CLAUDE.md — 퇴사노트 (toesa-note)

회사를 그만둔 뒤에 챙겨야 할 것들 — **실업급여·수급자격·조기재취업수당·건강보험**을
계산기 4종과 가이드 10편으로 정리하는 애드센스 수익형 미니사이트.
생활반장 노트 시리즈의 15번째 노트.

> 경계 주의: 급여노트(salary-note)가 **재직 중에 받는 돈**(실수령액·4대보험·퇴직금·주휴수당)을
> 다룬다면 이 노트는 **그만둔 뒤의 것**을 다룬다. 두 노트는 퇴사 시점을 기준으로 대칭이다.
> 퇴직금 계산기는 급여노트 몫으로 남기고 여기서 만들지 않는다 — 대신 가이드에서 링크한다.
> 연금노트와도 겹치지 않게, 국민연금은 "실업 기간의 처리"(납부예외·임의가입·실업크레딧)만
> 가이드로 다루고 수령액 계산은 연금노트로 보낸다.

## 스택·명령

- Next.js 16.2.10 (App Router) + TypeScript + Tailwind CSS 4. DB·로그인·결제·API 없음, 전부 정적.
- 개발 서버: 워크스페이스 `.claude/launch.json`의 `toesa-note-dev` (포트 4500)
- 빌드: `npm run build` / 테스트: `npm test` (vitest 74개)
- Node는 포터블: 명령 앞에 `$env:Path = "E:\클로드\tools\node;$env:Path"` 필요
- **빌드 성공 판정은 `$LASTEXITCODE -eq 0`으로 한다.** `✓ Compiled` 문자열은 타입 체크
  **전에** 출력되므로, 그걸로 판정하면 타입 오류가 있는 빌드를 성공으로 오인한다.
- 배포: `git push origin main` (Vercel 자동 배포)만 사용. 절차는 `DEPLOY.md`
- 도메인: toesa.lifebanjang.com (허브 `lib/notes.ts`에 등록)
- 테마: 보라(`--accent: #7a4ea3`) — 기존 15개 노트의 주황·파랑·초록 계열과 겹치지 않음

## 구조

- `lib/date.ts` — `formatWon` 등 공통 유틸
- 계산 엔진 + 테스트 (로직 수정 시 반드시 테스트 함께 갱신):
  - `lib/jobseeker.ts` 구직급여 — **이 노트의 핵심.** 평균임금 → 기초일액(임금일액 상한
    적용) → 60% → 상·하한으로 자름 → 소정급여일수(별표1) → 총액.
    `rateAppliesWageRange()`는 60%가 실제로 적용되는 월급 구간을 역산한다.
  - `lib/eligibility.ts` 수급자격 — 네 요건 판정 + **수급기간 만료 D-day**.
    `forfeitedDays()`가 늦게 신청해서 날아가는 일수를, `lastSafeApplyDate()`가
    전부 받으려면 언제까지 신청해야 하는지를 계산한다.
  - `lib/early-reemployment.ts` 조기재취업수당 — 다섯 요건 판정 + 남은 일수 × 1/2.
    요건 미달이어도 `potentialAmount`로 "얼마를 놓쳤는지"를 보여준다.
  - `lib/health-insurance.ts` 퇴사 후 건강보험 — 임의계속가입 보험료 + 신청 마감 D-day.
- 가이드 10편: `lib/guides-1.ts`(실업급여 본편 5편) · `lib/guides-2.ts`(퇴사 이후 5편)
- 계산기 페이지: `app/calc/{benefit,eligibility,early,health}/page.tsx`
  — SEO 해설 + `@graph`(FAQPage + BreadcrumbList) + `CalcNotes`(근거·예시·자주 틀리는 지점)
- 허브 역링크: `components/HubGuideLink.tsx` → `lifebanjang.com/guide/job-loss`

## 이 노트가 반드시 설명해야 하는 것 — 상·하한 역전

2026년 구직급여 **상한 68,100원**과 **하한 66,048원**의 간격이 **2,052원뿐**이다.
지급률 60%를 역산하면:

- 기초일액 **110,080원 이하**(월급 약 334만원 미만) → 무조건 하한액
- 기초일액 **113,500원 이상**(월급 약 344만원 초과) → 무조건 상한액
- 그 사이 좁은 구간에서만 60%가 실제로 적용된다

즉 **월급 200만원과 320만원인 사람이 1원도 다르지 않게 받는다.** "평균임금의 60%"라는
흔한 설명이 대부분 사실이 아니라는 뜻이다. 계산 결과에 `bound`(min/max/none)를 표시해
어느 쪽에 걸렸는지 드러내고, 홈·계산기·가이드에서 이 구조를 설명한다.
**이 설명을 빼지 말 것** — 이 노트가 다른 실업급여 계산기와 구별되는 지점이다.

## 주의사항 — 갱신 대상

> 전 노트의 갱신 항목과 월별 일정은 워크스페이스 `E:\클로드\CLAUDE.md`의
> **"기준값 갱신 캘린더"**에 모아 두었다.

- **구직급여 하한액** — 매년 1월. 최저임금 × 80% × 8시간.
  2026년 10,320 × 0.8 × 8 = **66,048원**. 최저임금이 바뀌면 자동으로 따라 바뀐다.
- **구직급여 상한액 / 임금일액 상한** — 고용노동부 고시. 2026년 **68,100 / 113,500원**.
  **2019~2025년 66,000원으로 7년간 동결됐다가 인상됐다.** 오래 묶여 있던 값일수록
  바뀐 것을 놓치기 쉬우니 연초에 반드시 확인할 것.
- **상한액은 이직일 기준으로 적용된다.** 신청일이 아니다. 그래서 `LIMITS_BY_YEAR`에
  연도별로 값을 보존하며, **옛 연도 값을 지우지 말 것.** 2025년 이직자가 2026년에
  신청하는 경우가 계속 발생한다.
- **건강보험료율·장기요양보험료율** — 매년 초 고시. 현재 본인부담 **3.595%** /
  건강보험료의 **13.14%**. **급여노트 `lib/insurance.ts`와 같은 값이어야 한다.**
  한쪽만 고치면 두 사이트가 다른 답을 낸다.
- **고시값 고정 테스트**: `lib/jobseeker.test.ts`와 `lib/health-insurance.test.ts`에
  값을 **리터럴로 박은** 테스트가 있다. 다른 테스트는 상수를 기호로 참조해 값이 낡아도
  통과하므로, **이 테스트가 실패해야 갱신을 놓치지 않는다.**
- 값을 고치면 **가이드 본문(`lib/guides-*.ts`)과 계산기 해설(`app/calc/*/page.tsx`)에
  그대로 쓴 숫자**도 함께 훑을 것. `Grep`으로 옛 숫자(예: `66,000`, `334만`)를 검색한다.

## 반영하지 않은 것 (의도적)

- **반복수급자 감액 규정** — 여러 차례 개정안이 논의됐지만 **확정된 시행 내용이 없다.**
  워크스페이스 CLAUDE.md의 "확정되지 않은 개편안은 넣지 않는다" 원칙에 따라 계산기에
  넣지 않고, 홈 FAQ에서 "확정 전이라 반영하지 않았다"고 밝힌다. 확정되면 반영할 것.
- **지역가입자 건강보험료** — 소득·재산·자동차 점수제라 점수표가 매년 바뀐다.
  어설픈 추정이 잘못된 판단을 부르므로 계산하지 않고 건보공단 모의계산으로 안내한다.
  이 방침을 바꾸지 말 것.
- **훈련연장급여·개별연장급여** 금액 계산 — 요건 판단이 고용센터 재량이라 가이드에서
  제도 설명만 하고 계산기로 만들지 않는다.

## 검증에 쓸 수 있는 기준값 (테스트에 들어 있음)

- 35세·월 280만원·가입 4년 → 일액 66,048원(하한) × 180일 = **11,888,640원**
- 52세·월 600만원·가입 12년 → 일액 68,100원(상한) × 270일 = **18,387,000원**
- 월 200만원과 월 320만원 → **일액이 같다**(둘 다 하한)
- 이직 2026-06-30 → 수급기간 만료 **2027-07-01**, 210일 전부 받으려면 **2026-11-26**까지 신청
- 210일 중 60일 수령 후 취업 → 조기재취업수당 **4,953,600원**
- 보수월액 350만원 → 임의계속 월 **142,350원**(건강 125,820 + 장기요양 16,530)

## 그 밖에

- 브라우저 스크린샷은 이 환경에서 타임아웃 가능 — get_page_text/read_page/javascript_tool로 검증.
- PowerShell에서 `app/guide/[slug]` 경로는 `-LiteralPath` 사용. `&&` 불가.
- `components/FamilyLinks.tsx`의 SITES 배열은 전 사이트 공통 정본 — 새 노트 추가 시 모든 사이트에 복사.
- **가이드를 추가·수정하면** 허브에서 `npm run gen:note-guides`를 돌리고 허브도 함께
  배포해야 `lifebanjang.com/articles/toesa` 목록에 반영된다.
- 입력한 급여·날짜는 브라우저에서만 처리하고 서버로 보내지 않는다 — 개인정보처리방침에
  명시된 대로 유지할 것.
- 노무·법률 자문이 아니라는 면책과 "최종 판단은 고용센터"라는 고지를 유지할 것.
